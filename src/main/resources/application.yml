

logging:
  level:
    root: info

spring:
  application:
    name: MybatisPlusDemo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis_plus?characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: xbh123456
